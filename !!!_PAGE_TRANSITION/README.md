# To develop

```bash
# View logs
docker logs http203-playlist

# Stop the container
docker-compose down

# Restart the container
docker-compose restart

# Rebuild if you make changes
docker-compose up --build -d
```
